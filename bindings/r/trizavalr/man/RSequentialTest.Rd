% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{RSequentialTest}
\alias{RSequentialTest}
\alias{$.RSequentialTest}
\title{A stateful sequential hypothesis test (mixture SPRT). Feed it
observations one at a time via \verb{$update(x)}; stop collecting data
as soon as \verb{$update()} returns \code{rejected = TRUE}.}
\usage{
RSequentialTest
}
\description{
A stateful sequential hypothesis test (mixture SPRT). Feed it
observations one at a time via \verb{$update(x)}; stop collecting data
as soon as \verb{$update()} returns \code{rejected = TRUE}.
}
\section{Methods}{

\subsection{Method \code{new}}{
Constructs a new sequential test.
\subsection{Arguments}{
\describe{
\item{\code{alpha}}{Desired Type I error rate, e.g. 0.05.}
\item{\code{tau}}{Prior standard deviation on the effect size worth detecting.}
}}
}

\subsection{Method \code{update}}{
Feeds one new observation into the test.
\subsection{Arguments}{
\describe{
\item{\code{x}}{The new observation (e.g. candidate_score - baseline_score).}
}}
\subsection{return}{
A named list with n, likelihood_ratio, and rejected.
}
}

\subsection{Method \code{current_mean}}{
Current running mean of observations fed so far.
}

\subsection{Method \code{n}}{
Number of observations fed so far.
}
}

